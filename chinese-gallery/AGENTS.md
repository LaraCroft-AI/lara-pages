# Описание проекта для ИИ-агентов

Этот файл — единая документация проекта `chinese-gallery`.

## Правила работы

- Храните все JSON-данные приложения в единственной папке `data` (строчными буквами); файлы npm остаются в корне.
- HTML содержит разметку; логику и обработчики размещайте в `scripts`, стили — в `styles`.
- Поддерживайте описание проекта и происхождение данных в этом `AGENTS.md`, не создавая дублирующих Markdown-файлов.
- Сохраняйте 214 ключей Канси, варианты написания, связи с уроками и совместимость существующих ссылок.
- После изменения путей проверяйте загрузку ресурсов с точным регистром имён. Для функциональных изменений запускайте соответствующие Playwright-тесты на desktop и mobile.
- Сохраняйте лицензии и сведения об источниках данных; не выдумывайте чтения или связи иероглифов.

## Назначение проекта

Статический учебный сайт с двумя разделами:

- **Уроки** (`vocabulary.html#lessons`): выбор школы (Жун, Рукодельникова или «Все школы») и нескольких словарей, поиск, озвучка и тренировка в обоих направлениях.
- **Карты** (`vocabulary.html#maps`): связи по основному ключу и по вхождению иероглифа в слово, фильтры HSK 3.0, поиск узлов, озвучка, режим списка и печатные карты/прописи.

## Запуск

```sh
cd chinese-gallery # из корня репозитория; пропустите, если уже в этой папке
npm start
```

Открыть `http://127.0.0.1:8765/vocabulary.html`. Сервер работает в текущем терминале без detachment; остановка — Ctrl+C. Требуется Python 3; `npm start` запускает `python3 -m http.server 8765 --bind 127.0.0.1`. Страница должна открываться через HTTP, поскольку словари загружаются через `fetch`. Сборка и серверная база данных не нужны. Tailwind и шрифты загружаются с CDN; озвучка зависит от поддержки китайского языка браузером/системой.

## Школы и уроки

Каталог `data/schools.json` задаёт школы (`id`, `name`) и их уроки (`id`, `title`). Идентификатор урока — уникальный ключ в `data/vocab_data.json`; одинаковые названия уроков у разных школ допустимы. Старые ключи «Урок 1»–«Урок 14» сохранены для Жун; `rukodelnikova-lesson-1`–`rukodelnikova-lesson-7` — семь уроков учебника Рукодельниковой. Названия школ и принадлежность уроков заданы пользователем.

По умолчанию выбрана школа Жун, уроки не выбраны. «Все школы» показывает все уроки с названиями школ и позволяет выбирать уроки нескольких школ одновременно. При смене школы сохраняется только выбор доступных в ней уроков; тренировка обновляется по новой выборке. Переход в «Карты» и обратно сохраняет школу и выбранные уроки. Карты используют слова всех школ независимо от выбора школы и уроков.

Для добавления урока добавьте массив слов в `data/vocab_data.json` и ссылку на его ключ в `data/schools.json`; каждому уроку должна соответствовать ровно одна школа. Для новой школы добавьте объект с уникальным `id`, названием и списком уроков. Скрипты обновления HSK и ключей обрабатывают все школы через общий файл словарей.

## Карты

Данные слов общие с уроками: `data/vocab_data.json` — 21 урок, 1049 записей: 768 в 14 уроках школы Жун и 281 в семи уроках школы Рукодельникова. Повторы между школами допустимы; внутри школы Рукодельниковой слово хранится только в самом раннем уроке, в согласованном списке которого оно встречается. Отдельных словарей «HSK 3.0 — Уровень …» нет; метки уровня у слов и фильтры карт сохранены. Отбор уроков не ограничивает карты. Фильтр HSK выбирает **ровно указанный уровень**, «Все» включает также слова вне списка HSK. Используется HSK 3.0, как в существующем словаре.

На карте показываются восемь слов за раз; остальные доступны через «Далее». «Список» отображает всю выборку. На узких экранах список выбран по умолчанию, карта доступна с горизонтальной прокруткой. Нажатие слова показывает полный перевод и запускает озвучку. Ссылку из адресной строки можно сохранить: она содержит тип, узел и уровень. Переход между разделами сохраняет выбранные уроки и текущую тренировку.

«Печатать карту» строит страницы с паутинками для всех выбранных слов, «Печатать прописи» — строки с образцами и клетками. В системном окне печати можно выбрать сохранение в PDF. Это собственные материалы на данных сайта; сторонние авторские PDF не распространяются.

Подписи и семейства ключей находятся в `data/map-catalog.json`. Справочник ключей — `data/character-radicals.json`, происхождение и лицензии описаны ниже. После добавления новых знаков пересоберите справочник скриптом `scripts/update_map_radicals.py`.

## Проверка

```sh
npm ci
npx playwright install chromium
npm test
```

Тесты выполняются на desktop (1280×900) и mobile (Pixel 5). Playwright автоматически запускает локальный HTTP-сервер на порту 8765. В CI (`.github/workflows/tests.yml` в корне репозитория) тесты запускаются при push в `main` и при создании PR; отчёты и снимки сохраняются при ошибке. `tests/vocabulary.spec.js` проверяет уроки; `tests/maps.spec.js` — переходы, связи и фильтры, поиск, пагинацию, отсутствие перекрытий, озвучку, печать и обработку ошибок загрузки.

Если данные не загружаются, проверьте доступность JSON-файлов через HTTP. В разделе «Карты» при ошибке есть кнопка повторной загрузки. Если не слышно произношение, проверьте наличие китайского голоса в системе. Для печати с иероглифами дождитесь загрузки шрифта.

Для пересчёта меток HSK 3.0 у слов используется `node scripts/update_hsk30.js /path/to/hsk30.csv /path/to/hsk30-expanded.csv`. Скрипт обновляет уровни в существующих уроках и не добавляет отдельные словари.

## Структура файлов

- `vocabulary.html` — разметка и подключения внешних файлов, без встроенных скриптов, стилей и обработчиков событий.
- `scripts/vocabulary.js` — уроки, карточки, поиск, озвучка и тренировки.
- `scripts/maps.js` — карты и связи иероглифов; оба браузерных скрипта подключаются в конце страницы в указанном в HTML порядке.
- `styles/vocabulary.css`, `styles/sections.css` — оформление уроков и карт. Порядок CSS-подключений сохраняет каскад исходной страницы.
- `data/vocab_data.json` — словари уроков; `data/character-radicals.json` — словарные ключи; `data/map-catalog.json` — каталог всех 214 ключей Канси и подписи иероглифов.
- `data/licenses/` — тексты лицензий данных.
- `scripts/update_hsk30.js`, `scripts/update_map_radicals.py` — обновление данных; пути по умолчанию определяются относительно скриптов и не зависят от текущей папки.
- `tests/`, `playwright.config.js`, `package.json`, `package-lock.json` — тесты и служебные файлы npm. JSON-конфигурация npm остаётся в корне.

Папка `data` пишется строчными буквами: это важно при размещении на сервере с учётом регистра путей. Для пересборки связей из корня проекта: `python3 scripts/update_map_radicals.py /path/to/dictionary.txt`.

## Происхождение данных

`character-radicals.json` связывает знаки из `vocab_data.json` с их основным словарным ключом. Это проекция полей `character` и `radical` из [Make Me a Hanzi, dictionary.txt](https://github.com/skishore/makemeahanzi/blob/master/dictionary.txt), полученная 16 сентября 2026 года. Исходный справочник основан на Unihan и CJKlib. Лицензия производного файла: LGPL-3.0-or-later; тексты LGPL и GPL находятся в `data/licenses/`.

Изменения относительно источника: удалены знаки, отсутствующие в словарях уроков, и все поля, кроме основного ключа; JSONL преобразован в JSON-объект. Для 阝 по полю `decomposition` восстановлен ключ: слева — 阜 (холм), справа — 邑 (город); при неоднозначной структуре скрипт останавливается. Проекция обновлена 17 сентября 2026 года. Графические данные и шрифты Make Me a Hanzi не включены. Связь означает основной словарный ключ, а не произвольное графическое сходство. Например, 月 (луна) и 肉/⺼ (плоть) различаются; 心/忄 и 火/灬 объединены в семейства.

`map-catalog.json` содержит все 214 ключей Канси: основную форму, пиньинь, русское значение, семейство форм, номер и число черт основной формы. Нумерация и основные формы соответствуют [Unicode CJKRadicals.txt](https://www.unicode.org/Public/UCD/latest/ucd/CJKRadicals.txt); число черт следует традиционным группам Канси (1–17). Русские подписи составлены для проекта. Варианты не увеличивают число ключей: например, 水/氵/氺 — один ключ №85, а 糸/糹/纟 — №120. Для поиска 阝 показаны оба семейства; связь конкретного иероглифа определяется положением компонента, а не совпадением начертания.

Слова, переводы, пиньинь слов и уровни берутся только из `vocab_data.json`; данные сайта-примера не включены. Карты иероглифов находят точное вхождение знака в слово, исключая сам отдельный знак. Каталог иероглифов включает каждый знак из уроков; если отдельного чтения и перевода нет, они не извлекаются из многосложного слова. Ключи без примеров остаются доступны, но печать слов для них отключена. Связанные иероглифы учитывают фильтр HSK; переходы ключ → иероглиф → ключ сохраняют этот фильтр. При добавлении уроков нужно обновить справочник ключей для новых знаков.

### Проверка пиньиня — 17 сентября 2026 года

Проверены 768 записей уроков, 214 ключей и 48 отдельных подписей иероглифов. Для сверки использован [CC-CEDICT](https://cc-cedict.org/editor/editor.php?handler=Download), для спорных чтений — [汉典](https://www.zdic.net/). Словосочетания без отдельной словарной статьи проверены по составляющим и значению. Словарные определения и полный сторонний словарь в проект не включены.

По выбранному учебному формату все слоги разделены одним обычным пробелом, в том числе внутри слова: `É luó sī`, `qǐ é`. Это послоговая учебная запись, а не стандартное орфографическое деление на слова. Нейтральный тон пишется без диакритики. Эризация не образует отдельного слога: `nǚ háir`, `xiǎo háir`; самостоятельное 儿 сохраняет `ér`, например `nǚ ér`. В `yú ér` сохранено послоговое чтение 鱼儿. Поиск принимает как пробелы между слогами, так и прежнее слитное написание и апостроф.

Исправления тонов и унификация чтений: 妈妈 `mā ma`, 哪里 `nǎ lǐ`, 狐狸 `hú li`, 蛐蛐 `qū qu`, 撒种 `sǎ zhǒng`, 队伍 `duì wǔ`, 道理 `dào li`, 鸳鸯 `yuān yāng`, 宽敞 `kuān chang`, 明亮 `míng liàng`, 骑自行车 `qí zì xíng chē`; 女孩儿 приведено к `nǚ háir`. У ключа №23 匸 исправлен тон: `xǐ` → [`xì`](https://www.zdic.net/hans/匸).

Основные тоны не заменяются контекстными изменениями: 一起 записано `yī qǐ` (произносится `yì qǐ`), 八个 — `bā gè` (в речи 个 может быть нейтральным). Это соответствует принципу записи исходных тонов в [GB/T 16159—2012](https://www.moe.gov.cn/ewebeditor/uploadfile/2015/01/13/20150113091717604.pdf). Не каждое расхождение словарей является ошибкой: сохранены допустимые полные тоны в 核桃 `hé táo`, 门把手 `mén bǎ shǒu`, 回来 `huí lái`, 烧饼 `shāo bǐng`. Для ключей сохраняются их специальные чтения, например 爿 `qiáng` и 黽 `mǐn`; последнее подтверждено [справочником Министерства образования Тайваня](https://stroke-order.learningweb.moe.edu.tw/dictMean.jsp?ID=40701&la=0).

Чтобы пересобрать производный файл из загруженного оригинала, выполните из корня проекта:

```sh
python3 scripts/update_map_radicals.py /path/to/dictionary.txt
```

Скрипт обновления поддерживает символ 〇 как `null` (без назначенного ключа), если он появится в уроках: в исходном справочнике его нет.


### Школа Рукодельникова — 18 сентября 2026 года

После проверки списка пользователь подтвердил добавление всех групп из урока 1 «Как дела? 你好吗?» (с. 12–21) учебного пособия М. Б. Рукодельниковой, О. А. Салазановой, Ли Тао «Китайский язык. Второй иностранный язык. 5 класс», 2017. Источник — предоставленный пользователем PDF `1rukodel_nikova_m_b_salazanova_o_a_li_tao_kitayskiy_yazyk_vto.pdf`; сам PDF и изображения страниц в сайт не включены.

В `rukodelnikova-lesson-1` добавлены 42 уникальные записи: новые слова и отдельно разобранные составляющие, дополнительные слова и имена (с. 13), базовые иероглифы (с. 18), лексика культуры и упражнения (с. 20–21). Повторы 上, 人, 大 объединены. Вариант 亻 указан в пояснении к 人, без отдельной карточки. Иероглиф ограды — 囗, не 口. Значение 丁 «гвоздь» сохранено как трактовка графемы в учебнике; у 师 отмечено употребление в составе слов. Вводный урок и случайные иероглифы заголовков не добавлялись.

Пиньинь приведён к послоговой записи проекта; нейтральные тоны 妈妈, 哥哥, 姥姥 сверены с CC-CEDICT. Чтения знаков упражнений сверены с тем же словарём; для 囗 выбран wéi, для 夫 — fū. [汉典: 囍](https://www.zdic.net/hans/囍) подтверждает xǐ и ключ 口; [囗](https://www.zdic.net/hans/囗) — чтение wéi. Уровни HSK пересчитаны штатным скриптом по закреплённой версии ivankra/hsk30 из комментария скрипта; номер урока не используется как уровень HSK.

Справочник ключей пересобран из Make Me a Hanzi. Для отсутствующего в нём 囍 скрипт содержит явное дополнение 囍 → 口 по 汉典. Остальные связи по-прежнему берутся из исходного справочника, лицензии сохранены.


### Уроки 2–7 Рукодельниковой — 18 сентября 2026 года

По предоставленному PDF визуально проверены с. 22–100: урок 2 (22–33), урок 3 (34–48), урок 4 (49–63), урок 5 — повторение (64–73), урок 6 (74–85), урок 7 (86–100). Пользователь проверил перечень и поручил добавить все группы без повторов. Включены новые и дополнительные слова, отдельно разобранные иероглифы, имена, культурные примеры и согласованный материал упражнений. Фонетические упражнения без иероглифической записи и служебные заголовки не превращались в словарные записи.

Добавлено 239 записей: уроки 2–7 содержат соответственно 26, 56, 51, 20, 35 и 51 запись. С уроком 1 (42) всего 281 уникальная запись. Повторы удалены в пределах всей школы Рукодельниковой, включая урок 1; уроки Жун не изменены. Поэтому слово, повторённое в учебнике позднее, остаётся доступным в более раннем уроке. Дополнительные значения 子, 未 и 午 из раздела о времени сохранены в пояснениях к ранним записям.

Традиционные 魚, 車, 轟 из задания на с. 73 сведены к карточкам 鱼, 车, 轰 с исходной формой в переводе. Компоненты без самостоятельного чтения (讠, 氵, 彳, 宀, 冖, 辶, 忄, 爫, 勹, 刂 и графические варианты) отражены в пояснениях, а не как новые слова с вымышленным пиньинем. Для 阝 сохраняется различие: слева 阜, справа 邑; у 都 ключ справа.

Чтения слов сверены с CC-CEDICT; учебные имена и составные числительные — со страницами PDF и составляющими. 艮 в значении «крепкий» имеет чтение gěn (не gèn, относящееся к триграмме). У имён сохранены прописные буквы, у эризации — nǎr/nàr без отдельного слога er. 夜里 сохранено с полным тоном lǐ, как на с. 99; послоговые пробелы и исходные тоны 一/不 следуют общему формату проекта. Метки HSK пересчитаны по прежнему справочнику на основе написания слова; они не различают омографические значения и собственные имена. Проекция ключей пересобрана из Make Me a Hanzi с существующим дополнением для 囍; охвачены все 880 знаков текущего словаря.
